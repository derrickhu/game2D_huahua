---
description: 花店室内家具（subpkg_deco/furniture）NB2 生图 — 与房间壳一致的等距视角；配色按单品需求；入库须压缩并注册 TextureCache
globs: docs/prompt/furniture_*.txt,minigame/subpkg_deco/images/furniture/**,minigame/subpkg_deco/images/cake/**,src/config/DecorationConfig.ts,src/utils/TextureCache.ts
alwaysApply: false
---

# 室内家具贴图 — 画风与视角规范（花花妙屋）

凡新增或重绘 **`minigame/subpkg_deco/images/furniture/`** 下、用于花店 **L 形房间** 内摆放的家具单件贴图，须与当前 **房间背景壳**（`house_bg_room_*`）及已验收参考批次 **花房盆栽线**（`greenhouse_pot_*`）、**柔色长凳**（`orn_pastel_bench`）等在 **视角与线稿气质** 上一致；**配色不强制马卡龙**，可按房间主题、单品设定使用多样色相与饱和度，只要仍是 merge 花店道具可读剪影即可。

## 视角（与房间一致）

### 两套「角度」勿混用（强制）

| 说法 | 含义 | 正确数值 |
|------|------|----------|
| **相机 / 视线** | 从左上俯看 L 形房间的描述 | prompt 可写 **upper-left isometric camera**（旧文「~45°」仅指相机方位，**不是**墙在画面上斜 45°） |
| **屏上几何** | 墙脚线、地坪菱形边、家具顶面左右边相对水平线的倾角 | 当前验收房壳约 **±30°**（真等距）；接近 2:1 伪等距的 ±26.6° 也可，**禁止**把墙脚/台面边画成 ±45° |

实测锚点（`bg_room_default_soft_nb2` / `bg_room_candy_nb2`）：后墙墙脚约 **-30°**，右墙墙脚约 **+30°**。家具顶面左右边应与此同族，左右对称；验收时若一侧偏平（如 +15°）或过陡（如 -47°），须重生。

### 地面家具（道具角）

- 使用 **2.5D 真等距**：相机 **upper-left**，与房间壳同一条视线；屏上顶面菱形边 **≈ ±30°**。
- 须读出 **顶面 + 朝向右下/右前的立面**，与「后墙 + 右墙」L 形空间一致；竖边保持 **竖直**。
- **锁角参考图（强制）**：生地面家具时 `--image` **必须**附  
  **`docs/prompt/refs/furniture_iso_grid_pm30.png`**  
  （白底 ±30° 菱形网格 + 中央示意方块；只锁透视，不锁配色/造型）。  
  可选再附自家房壳裁切（墙脚+地坪）作第二参考；**禁止**用外游完整样板间当主 `--image`（易抄风格与家具造型）。
- Prompt 须写明：*Use the attached grid ONLY for isometric angle (±30° top edges, vertical height edges); do not copy the gray demo cube.*
- **禁止**：纯正面平视、纯侧视、强烈透视广角、室内全景带墙（家具图仅为 **单件道具**）；禁止把「45°」理解成墙面/台面边斜 45°。

### 墙饰例外

墙饰 / 挂帘走下文 **「墙饰 / 窗帘」** 节：贴墙面平行，**不是**地面道具角；**不要**附 `furniture_iso_grid_pm30.png`，改附已验收墙饰锁角图。

## 画风（锚点：花房盆栽线 + 长凳批次 — 视角与线稿为主）

以下与 `docs/prompt/furniture_batch10_meta.txt`、`furniture_greenhouse_batch_meta.txt` 及 **`furniture_orn_pastel_bench_nb2_prompt.txt` / `furniture_greenhouse_pot_*_nb2_prompt.txt`** 在 **透视与笔触** 上对齐；**色彩可按策划单独立项**，不必复刻马卡龙或高明度 pastel。

| 维度 | 要求 |
|------|------|
| 品类气质 | **Hand-painted mobile merge-game flower shop**，偏 **kawaii / chibi-friendly** 道具比例，非写实照片、非厚重 3D 渲染。 |
| 线稿 | **Soft brown outlines**（略粗于头发丝、圆润转角），与房间壳 *soft brown line art slightly thicker than hairline* 一档。 |
| 色彩 | **按单品/房间主题自选**：可深可浅、可饱和可柔和；木质、金属、布艺等材质真实可信即可。**不强制**马卡龙或 bright pastels；需与房间木作/瓷砖呼应时在提示词中点明即可。 |
| 光照 | **Upper-left 主光**，柔和高光 + 轻微体积感；避免整图聚光灯圆斑、重暗角。 |
| 剪影 | **Readable silhouette**，小尺寸在装修格子内仍可辨认。 |
| 盆栽类 | 花材 **可爱风格化**，与鲜花合成线意象一致即可，**勿**写实摄影盆栽。 |

## 画布与背景

- **比例**：**1:1 方图**，单件道具 **居中**，四周 **留白**（提示词常用 *generous margin*）。
- **背景**：**纯实心白 `#FFFFFF`**，供 rembg / 抠图流水线使用；**禁止**灰底、渐变底、地面投影条带假装地面。
- **禁止**：文字、水印、角色、二维码；需要「招牌字」类内容时用抽象图形替代或留空。

## 提示词写法（英文正文）

1. 每条家具 **独立** `docs/prompt/furniture_<id>_nb2_prompt.txt`，**仅英文正文**（不要 `#` 注释行，避免打进 API）。
2. 建议结构：**画布句**（Square 1:1…）→ **视角句**（见下「视角句范本」）→ **风格句**（soft brown outlines + 该件具体配色与材质…）→ **主体描述** → **禁忌句**（No text…）→ **背景句**（Background pure solid white #FFFFFF…）。
3. 与房间木作、窗框颜色需呼应时，可点明 *matching pink-tile / warm wood flower shop rooms* 等，但勿把整间房间画进图里。

**视角句范本（地面家具，推荐置顶）**：

```
Cute kawaii 2.5D true isometric furniture for a flower-shop merge game. Camera from upper-left into the L-room corner (same family as the room shell). On-screen top-face edges and base edges must sit near ±30° from horizontal — match the attached iso grid and room wall-base / floor diamond, NOT 45° wall slant, NOT flat front view. Height edges stay vertical. Visible top surface plus front-right side, left and right top edges symmetrically angled. Use the attached grid ONLY for perspective; do not copy the gray demo cube.
```

## 入库与流程

- 原图、脚本与 `TextureCache` / `DecorationConfig` 登记见 **`docs/furniture_greenhouse_nb2.md`**、`game-art-asset-flow.mdc`（**deco 分包**，非 `subpkg_items`）。
- 抠图默认 **`birefnet-general`**，裁切 **`crop_trim.py --padding 4`**（与花房批次 README 一致）；`defaultScale` 在 `DecorationConfig` 中按格子单独调校。
- **强制注册 TextureCache**：凡新增/替换可购买或可摆放家具，`DecorationConfig.ts` 中 `icon` 必须与 `src/utils/TextureCache.ts` 中同名 key 一一对应，并指向真实 PNG 文件；房间风格 `bgTexture` 同理。只把 PNG 放进 `minigame/subpkg_deco/images/...` 或只写 `DECO_DEFS` 都不算完成，会导致发版后购买卡片/装修托盘取不到贴图。
- **入库前必须跑严格校验**（仓库根）：

```bash
npm run check:deco-textures
```

该脚本会扫描 `DecorationConfig` 的全部 `icon` / `bgTexture`，检查 `TextureCache` key 是否存在、映射文件是否真实存在；有任何缺失都必须先修复再发版。

## 放置与深度排序（生图 + 注册 — 强制）

房间**没有**「吸附到桌面」的物理碰撞；玩家自由拖拽，**能否看起来摆在桌子/柜台上、地毯是否永远贴在最底层**，全靠 `DecorationConfig` 深度字段 + `defaultScale` + 生图脚点。**设计新家具时先归类，再写 prompt、再登记配置**（见 `RoomDepthSort.ts`、`getDepthSortTypeLift` / `getDepthSortFeetYFudge`）。

### 四类放置意图（设计阶段先选一类）

| 放置意图 | 典型品类 | `DecoSlot` | `DecorationConfig` 必填/推荐 | 生图注意 |
|----------|----------|------------|------------------------------|----------|
| **A. 台面小物** | 花瓶、蜡烛、玩偶、果盘、大捧花、台面家电 | `ORNAMENT` 或 `LIGHT` | 见下表「台面小物」 | 脚点在**物体最底**（瓶底/托盘底）；**勿**画地面投影条、勿画整片假地板 |
| **B. 贴地地毯 / 地垫** | 圆毯、长毯、针织毯、星纹毯 | `ORNAMENT`（常归 furniture / flower_room Tab） | `depthSortFloorMat: true`，`depthSortYLift: 0` | 俯视**平铺**感、薄毯体；无立体桌腿高度 |
| **C. 地面大件** | 沙发、床、长凳、展示柜、落地钟 | `ORNAMENT` / `TABLE` / `SHELF` | 一般**不设** depth 覆盖；`defaultScale` 常 **> 0.92** | 完整落地体量，脚点贴地 |
| **D. 墙饰 / 庭院** | 挂画、窗帘、户外花架 | `WALLART` / `GARDEN` | 墙饰见下文「墙饰例外」；庭院按地面大件 | 墙饰贴立面透视，非台面逻辑 |

### A. 台面小物 — 配置规则

引擎默认：`ORNAMENT` 且 `defaultScale ≤ 0.92` 会自动获得 `depthSortFeetYFudge: 95` 与 `depthSortYLift: 140`。**但** `decorationPanelTab: 'furniture'` 且 **`defaultScale > 0.92` 会被当成地面大件（补偿为 0）**——大捧玫瑰、大花瓶等「视觉偏大、却要摆桌面」的件**必须显式写**：

```typescript
depthSortFeetYFudge: 95,   // 小摆件默认；偏大或 LIGHT 台面件可用 110～130
depthSortYLift: 140,       // 同 Y 带内略压过桌沿；显式 0 表示按地板大件
```

参考：`orn_vase`（0.53，自动台面）、`modern_hotdog_roller`（LIGHT + `depthSortFeetYFudge: 130`）、`workshop_giant_rose_bouquet`（1.15 + 显式 110/140）。

- **`defaultScale`**：台面小物优先 **0.45～0.92**；若美术需要更大，保留大 scale 但**仍加**上述 depth 字段。
- **验收**：编辑模式拖到 `table_round_cafe`、厨柜、茶几上，确认**不被桌面挡住**、脚点视觉上在台面而非地板。

### B. 地毯 / 地垫 — 配置规则

**必须**同时满足，否则地毯会盖住桌椅、店主脚：

```typescript
depthSortFloorMat: true,
depthSortYLift: 0,
```

参考：`orn_greenhouse_rug`、`dream_cloud_star_rug`、`azure_lazy_star_border_rug`。

- **禁止**对地毯设置 `depthSortFeetYFudge`（贴地排序，不需抬脚点）。
- **验收**：与同区域沙发/桌几重叠时，地毯恒在**最底层**（仅高于房壳），不挡人物腿。

### 入库检查清单（`DECO_DEFS` 新行必过）

1. 已选放置意图 A/B/C/D，且与策划描述一致（「可摆台面」≠ 默认地面大件）。
2. 台面小物：若 `furniture` Tab 且 `defaultScale > 0.92` → 已写 `depthSortFeetYFudge` + `depthSortYLift`。
3. 地毯类：已写 `depthSortFloorMat: true` 且 `depthSortYLift: 0`。
4. 地面大件：未误加台面补偿（避免整件浮在桌面前面）。
5. 游戏内编辑模式实测：台面件能摆上桌子；地毯不挡家具/人物。

## 压缩与像素规格（须对齐现有家具）

- **像素（普通购买线 / Lv 家具）**：等比缩放后 **最长边不宜超过 171 px**（与 `scripts/normalize_greenhouse_furniture_pngs.py` 中 `MAX_SIDE` 及当前多数已验收家具一致）。勿将未缩放的高分辨率生图直接长期留在 `furniture/`。
- **编码**：RGBA PNG，`optimize=True`、`compress_level=9`（与花房归一化脚本写入方式一致）；**不使用**棋盘物品那套 256 色量化（`compress_subpkg_items_pngs.py`），以免装修大图色带。
- **体积参考**：同目录现有成品中，简单小件约 **8～20 KB**，桌架类复杂件常见 **35～55 KB**；若同类单品 **显著偏大**（例如远大于 100 KB 且无特殊理由），应检查是否未缩放或未优化。
- **入库前整目录（推荐，仅普通家具）**：

```bash
python3 scripts/compress_furniture_deco_pngs.py
# 或先看变化：
python3 scripts/compress_furniture_deco_pngs.py --dry-run
```

## 工坊家具清晰度（`workshopExclusive`）

工坊大件在房间里 **`defaultScale` 往往 ≥ 1.7**，若仍按 **171 px** 入库，手机上放大后会发糊。**验收标杆**：弧翼大沙发三色合图 `workshop_plush_sofa_sheet`（每格 **342 px**）——真机已够清晰，**不必再提到 512**。

**与普通家具的唯一差别**：入库时 **`--max-side 342`**，其余流程相同（rembg → crop_trim → 合图/压缩）。

| 类型 | 命令要点 |
|------|----------|
| 合图（正/背或多配色） | `process_furniture_atlas_sheet.py --max-side 342 …` → `compress_furniture_deco_pngs.py --force` |
| 单张 PNG（如床） | `compress_furniture_deco_pngs.py --max-side 342 --force <path>` |

**注意**：工坊 PNG 入库后**勿**再跑默认 `--max-side 171` 的整目录压缩，否则会被压糊。

```bash
# 示例：1 列 × 3 行多配色（弧翼沙发）
python3 scripts/process_furniture_atlas_sheet.py \
  --compose .../green.png --compose .../sakura.png --compose .../blue.png \
  --columns 1 --rows 3 --max-side 342 --padding 4 \
  -o minigame/subpkg_deco/images/furniture/workshop_plush_sofa_sheet.png
python3 scripts/compress_furniture_deco_pngs.py --force minigame/.../workshop_plush_sofa_sheet.png
```

## 反例（勿做）

- 棋盘 **工具图标** 那套 **3/4 俯视小物件**（见 `tool-icon-art-style.mdc`）— **家具用等距花店风，二者勿混用为同一参考图。**
- 写实 IKEA 照片风、暗黑低饱和、细黑线稿扁平 UI 图标风。
- 带第四面墙或透视地板的「小场景」而非单件家具。

## 「免费赠予」家具（熟客/活动/任务奖励）特例

凡通过**非花愿购买**路径获得的家具（当前为熟客 Bond Lv4 主题家具 `affinity_*`，未来活动 / 主线任务奖励家具同理）须遵循以下额外约束：

| 维度 | 要求 |
|---|---|
| `cost` | **0**（不走花愿购买） |
| `starValue` | **必须 0** — 否则可被「摆放→拆除→重摆」反复刷星，破坏星星只来自购买行为的经济循环 |
| `rarity` | **`LIMITED`**，且**整体细节密度、材质感**应明显高于同 slot 的常规购买家具（黄铜/玫瑰金/胡桃木/雕花/玻璃柜/雕花铁艺等），让玩家一眼读出**稀缺感**而不是「免费 = 朴素」 |
| `unlockRequirement` | 带 `questId` + `conditionText` + `questDetailText`，由对应 Manager（如 `AffinityManager._grantMilestoneReward`）通过 `grantQuest()` + `DecorationManager.gmUnlockDeco()` 一并发放；**不要**走 `unlock()` 购买路径 |
| 资源路径 | 推荐独立子目录（如 `subpkg_deco/images/affinity/`），避免与购买线 `furniture/` 混为一谈，便于后续批量重绘/替换 |
| 压缩 | 入库前显式给路径跑 `python3 scripts/compress_furniture_deco_pngs.py --force <path/*.png>`（脚本默认只扫 `furniture/` 根，子目录需显式传文件） |

注释模板（写在 `DECO_DEFS` 该批次行上方）：

```
// 注：starValue 必须为 0 — 这些是免费赠予的稀有家具，若计星会被「摆放→拆除→重摆」刷分；星星统一来自购买行为。
```

## 多件批量生图的两种姿势

1. **单件单提示词（首选 / 风险最低）**：每件 1:1 512px 单图，**避免** grid 切割误伤边缘（曾在熟客家具批次出现：trophy case 右框、balcony rack 铁艺纹被切到相邻 cell）。文件命名 `furniture_<id>_nb2_prompt.txt`，输出 `../game_assets/huahua/assets/raw/<id>_nb2.png`，直接 rembg → crop_trim → ship，无 split 步骤。
2. **批量 grid（仅用于强风格统一 + 单件偏小的场景，如盆栽线）**：在 prompt 里**显式**要求每个 cell 的 prop 只占 cell 中央约 **55%–60%**，四周留宽白边；`split_grid.py --margin 0`（让边缘安全带留给 prop），同时核对生成图每个 cell 的 silhouette 都收敛在 cell 内，再走 rembg 流程。**禁止**对体量较大、轮廓饱满的家具（带玻璃柜、雕花花架、长条桌等）走 grid 出图——优先走方案 1。

## 家具雪碧图合图（同族多形态 — 强制）

**同一家具族的多种形态必须写入一张 PNG 雪碧图（sprite sheet）**，运行时由 `FurnitureRenderConfig.ts` + `TextureCache` 按帧切分；**禁止**为同一族的正/背、多配色、可交互态各放独立 PNG（除非历史兼容且尚未迁移）。

### 布局约定

| 维度 | 列（col） | 行（row） |
|------|-----------|-----------|
| **四向旋转（fourFacing）** | col **0 = 正面**（front），col **1 = 背面**（back）；左右向由引擎 **水平翻转** 正/背帧 | 默认 **row 0**；多配色时 **每种配色占一行** |
| **多配色（同族）** | 仍用 front/back 两列 | row 0 = 默认色，row 1 = 分色（例：弧翼沙发 default + sakura → 同一 `{family}_sheet.png` 两行） |
| **可交互态** | 若仍四向：每种交互态 **占一行**，列仍为 front/back | 例：row0=默认关，row1=打开 |
| **不可旋转 + 多态** | 按列扩展各态 | 单行 |

- **合图文件**：`minigame/subpkg_deco/images/furniture/{family}_sheet.png`
- **TextureCache key**：`{family}_sheet`；子帧：`{sheetKey}#{col},{row}`
- **DecorationConfig.icon**：仍写 **deco id**；托盘/卡片默认 **正面帧**（别名）
- **工程**：`FurnitureRenderConfig.ts` 登记 `renderMode` + `atlas`；`TextureCache` 登记 sheet key

### 生图（Gemini / NB2）

- **四向家具**：**一张图生成正+背**（2 列 × 1 行），白底宽缝，同模型、同等距视角、同尺度；范本：`furniture_workshop_puffy_petal_sofa_front_back_sheet_nb2_prompt.txt`
- **附参考图**：生成背面时 **必须附正面已验收图**（`--image`）
- **比例**：合图整体 **1:1**（512×512）
- **抠图**：整图 **`birefnet-general`** → `crop_trim --padding 4`
- **压缩**：与普通家具相同，`compress_furniture_deco_pngs.py --force`；`*_sheet.png` 不会整图缩到 171（见脚本 `sheet-optimize`）
- **合图尺寸**：工坊 **`--max-side 342`**（弧翼沙发三色合图为标杆）；普通 Lv 家具 **171**

### 运行时

- `fourFacing` → 编辑栏 **「旋转」**（非镜像翻转）
- 预加载：`collectFurniturePreloadKeys()` 只拉 sheetKey

## 反例（勿做）

- 棋盘 **工具图标** 那套 **3/4 俯视小物件**（见 `tool-icon-art-style.mdc`）— **家具用等距花店风，二者勿混用为同一参考图。**
- 写实 IKEA 照片风、暗黑低饱和、细黑线稿扁平 UI 图标风。
- 带第四面墙或透视地板的「小场景」而非单件家具。
- **同一家具族** 正/背/分色/交互态 **各一张独立 PNG**（应合并为 `{family}_sheet.png`）。

## 墙饰 / 窗帘（`DecoSlot.WALLART` — 视角强制）

地面家具/摆件用 **真等距道具角**（顶面 + 立面，屏上边 ≈ ±30°）；**墙饰、挂帘、挂画、标本框** 须 **与房壳后墙 / 右墙立面平行**，贴在墙面上 — **不是** 地面道具角，**不是** 正面 billboard。

| 要求 | 说明 |
|------|------|
| **墙面平行（强制）** | 整件道具落在 **单一竖直墙平面** 上；杆/框上沿 **左高右低** 轻斜（与后墙墙脚同族，约 **-30°** 量级）；**禁止** 横平竖直正面杆、**禁止** 地面家具式顶面菱形道具角、**禁止** 杆向右上翘或任意斜向 |
| 透视句 | prompt 须含 **「parallel to back wall plane / lies flat on vertical wall / top edge slopes down toward the right」**；范本：`wallart_*_isometric_nb2_prompt.txt`、`deco_late_lv7_wall_01_isometric_nb2_prompt.txt` |
| 参考图（锁角） | 生图 **必须** `--image` 附已验收墙饰：**`deco_lv20_wall_moon_sheer_curtain.png`**（挂帘）或 **`wallart_frame.png`**（挂画/标本框）；**只锁透视角**，主体设计写在 prompt 正文 |
| 比例 | 挂帘 **3:4**；宽画/三联框 **4:3**；方画 **3:4** 或 **1:1**（见 `gen_wallart_isometric_nb2.sh`） |
| 脚本 | 批量：`scripts/gen_wallart_isometric_nb2.sh`；单件 **`gemini-3.1-flash-image-preview`** + 参考图 |
| 入库 | rembg **`birefnet-general`** → `crop_trim --padding 4` → **`compress_furniture_deco_pngs.py --max-side 171 --force`** |
| 反例 | 纯正面窗帘/画框、地面等距角、杆水平、与参考墙饰角度不一致（进房后显「歪」） |
| **倾角微调（推荐）** | 生图后若帘杆偏平/过陡：用 **`scripts/adjust_wallart_angle.py`** 对齐到目标角（如 `--target 27.5`），或打开 **`tools/wallart_angle_editor.html`** 拖图可视化微调后导出。分色合图前先统一各色顶边角，比反复 NB2 更稳 |
