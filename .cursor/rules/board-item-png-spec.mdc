---
description: 棋盘格物品图（items 分包）统一尺寸与 PNG 压缩 — 入库前强制流程
globs: minigame/subpkg_items/images/**/*.png, scripts/compress_subpkg_items_pngs.py, src/utils/TextureCache.ts, docs/prompt/*_nb2_prompt.txt
alwaysApply: true
---

# 棋盘格物品 PNG 规范（`subpkg_items/images`）

凡落入 **`minigame/subpkg_items/images/`**、在棋盘上以 `ItemView` / `TextureCache` **items 分包** 显示的图标（鲜花、饮品、工具、宝箱、红包、棋盘消耗品等），**入库 Git 前**须满足下列压缩标准，与当前 **幸运金币** 体量一致（约数 KB 级、最长边 128px）。

## 强制流程

1. **白底合图切格**（5×2、4×2 等 progression 表）：**禁止**仅用 `宽÷列数` 等分（易切掉左右）。用仓库脚本按行内白缝定位，再对内容 bbox **左右多留边**（默认 `--hpad 32 --vpad 16`）：
   ```bash
   python3 scripts/split_sheet_row_by_gutter.py \
     ../game_assets/huahua/assets/raw/{sheet}.png \
     --row 0 --index 4 \
     -o ../game_assets/huahua/assets/split/{name}.png
   ```
2. **抠图**：**rembg `isnet-anime`**（棋盘物品线默认；2D 插画 / 白底合图图标；见 `remove-background-game-art.mdc` 分工）→ **`crop_trim.py --padding 8`**（棋盘花束类建议 8，其它小图标可用 4）。
3. 拷贝到 `minigame/subpkg_items/images/` 下对应子目录，`TextureCache` 的 `ITEMS_IMAGE_MAP` 已注册 key。
4. **整目录跑压缩脚本**（仓库根）：

```bash
python3 scripts/compress_subpkg_items_pngs.py
```

- 可先 `python3 scripts/compress_subpkg_items_pngs.py --dry-run` 看体积变化。
- 仅新增/替换少数文件时，仍建议对**整个** `subpkg_items/images` 执行一次，保证与全量规范一致。
- 脚本将中间 PNG 写入**系统临时目录**，不会在 `subpkg_items` 下产生 `tmp*.png`；若曾在资源目录内写临时文件，微信开发者工具可能缓存该路径并在文件消失后报 `ENOENT`。

## 脚本等价参数（勿在业务代码里重复实现另一套）

| 项 | 值 |
|----|-----|
| 最长边上限 | **128 px**（`--max-side 128`），不足则不放大 |
| 缩放算法 | LANCZOS |
| 调色板 | **256 色**，`FASTOCTREE` + **Floyd–Steinberg** 抖动 |
| PNG | `optimize=True`，`compress_level=9` |

## 例外

- **`special/special_lucky_coin.png`**：脚本**默认跳过**，保持既有 P 模式小图；勿随意覆盖。
- 若个别资源验收不通过，可临时调高 `--max-side` 或改脚本参数，但**须同步更新本规则与脚本头部说明**，避免规范漂移。

## 相关文件

- 白底合图切格：`scripts/split_sheet_row_by_gutter.py`
- 压缩：`scripts/compress_subpkg_items_pngs.py`
- 纹理映射：`src/utils/TextureCache.ts` → `ITEMS_IMAGE_MAP`
- 示例（3 级花束）：`bouquet_line_rework_highsat_nb2_v2.png` → row0 index2 → rembg **`isnet-anime`** → `flower_bouquet_3.png`
- 示例（5 级花束）：同表 row0 index4 → rembg **`isnet-anime`**（若验收更优可保留；历史曾用 `birefnet-general` 可单张重抠对比）
