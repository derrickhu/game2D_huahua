---
description: 花花妙屋 AI 资产生图（NB2）与入库到 game_assets 再拷贝进 minigame 的固定流程
alwaysApply: true
---

# 游戏资产：NB2 生图与入库（固定流程）

当任务涉及「用 AI 生成游戏用图并替换工程内资源」时，按以下顺序执行，**不要跳过落盘与拷贝步骤**。

## 路径约定

**强制**：凡 AI 生成的**原图**（NB2、Cursor GenerateImage、或其它工具直出、**尚未**按规范抠图/裁切/压缩写入 `minigame/` 之前），**必须**落盘到 **`../game_assets/huahua/assets/`** 下表约定子目录（默认 **`raw/`**）。

**禁止**将原图作为**唯一或长期**存放位置：

- Cursor 工作区下的 **`.cursor/`** 目录（含 **`.cursor/projects/**/assets/`** 等编辑器/会话生成的 `assets` 路径）
- 仅留在 **`/tmp`**、下载夹、或本仓库内临时路径而不拷贝到 `game_assets`

禁止跳过归档直接覆盖 `minigame/`（避免丢失可再加工的源文件）。

**推荐做法**：NB2 / `generate_images.py` 一律用 **`--output ../game_assets/huahua/assets/raw/{任务名}.png`**；若工具只能先写到别处，**生成完成后第一步**即 `cp`/`mv` 到上述 `game_assets` 路径，再抠图入库。

| 用途 | 路径 |
|------|------|
| 提示词（入 Git 仓库） | `{workspace}/docs/prompt/{任务名}_nb2_prompt.txt` |
| 生成原图（仓库外资产库，默认） | `../game_assets/huahua/assets/raw/{任务名}.png`（自 `{workspace}`） |
| 游戏内运行时资源 | `{workspace}/minigame/subpkg_deco/images/...`（以 `TextureCache` 或策划路径为准） |
| **棋盘物品图标（items 分包）** | `{workspace}/minigame/subpkg_items/images/...`；入库前**必须**按 **`board-item-png-spec.mdc`** 运行 `scripts/compress_subpkg_items_pngs.py` |
| 可选：与旧 sync 脚本对齐 | `../game_assets/huahua/assets/house/bg.png` 等 |
| **UI / 界面原型图（非运行时）** | `../game_assets/huahua/assets/ui_prototypes/`（含 `*_prompt.txt`）；**勿**将此类 PNG 放入本仓库的 `minigame/` 或 `docs/design/` |

`{workspace}` 为本仓库根目录（花花妙屋 huahua）。

**仓库外资产库**：与本仓库**同级**目录下的 `game_assets/huahua/`（任意机器、任意用户名均适用）。下文 shell 示例均假定在 `{workspace}` 下执行，故写作 **`../game_assets/huahua/...`**（若你的资产库不在同级，可设环境变量 `GAME_ASSETS_HUAHUA` 指向实际根路径，脚本侧与此对齐）。

界面整页原型建议按 **9:16** 手机游戏画幅生成（`--aspect-ratio 9:16`）。

**NB2 导出分辨率（默认）**：**图标合图、精灵表、多格按钮表、小控件底板**等（非全屏弹窗壳）统一按 **512×512 正方形** 出图：`--aspect-ratio 1:1`，入库前若模型输出边长偏差，缩放到 **512×512**（或最长边 512、等比居中）。全屏背景 / 竖版面板壳体仍用 **9:16** 等任务约定比例。

## 模型与命令

- **NB2** = `gemini-3.1-flash-image-preview`（gemini-image-gen skill）。
- 竖版全屏背景常用 `--aspect-ratio 9:16`；**512 合图**用 `--aspect-ratio 1:1`（见上条）。
- 使用 **prompt 文件** 避免超长命令行：

```bash
# 在仓库根执行
mkdir -p ../game_assets/huahua/assets/raw

python3 ~/.cursor/skills/gemini-image-gen/scripts/generate_images.py \
  --prompt-file "docs/prompt/{任务名}_nb2_prompt.txt" \
  --output "../game_assets/huahua/assets/raw/{任务名}.png" \
  --model gemini-3.1-flash-image-preview \
  --aspect-ratio 9:16
```

## 抠图（透明底）

若资源需去底 PNG：**强制**遵循 **`remove-background-game-art.mdc`**：**默认 BiRefNet**（`rembg` **`-m birefnet-general`**，脚本在 **`~/.cursor/skills/remove-background/scripts/`**）。**禁止**以色键/魔棒/颜色阈值作为入库终稿；**除非特殊需要**且已在说明或 PR 中写明例外（规范全文见该规则）。

## 入库到游戏

生成成功后 **再拷贝** 到工程内目标路径（示例：花店室外背景）：

```bash
cp "../game_assets/huahua/assets/raw/house_yard_bg_nb2.png" \
   "minigame/subpkg_deco/images/house/bg.png"
```

- `prompt.txt` 内**只写发送给模型的正文**，不要夹带 `#` 注释行（会一并提交给 API）。
- 流程说明、任务元数据写在 `docs/游戏资产生图与入库流程.md` 或本规则中即可。
- API/代理失败时：可改用 Cursor `GenerateImage` 等兜底；**同一工作流内须立即** `cp`/`mv` 到 `../game_assets/huahua/assets/raw/{任务名}.png`（或与下表一致的子目录），**不得**仅以 Cursor `.cursor/.../assets/` 下的文件为准；然后再执行抠图/裁切后拷贝进 `minigame/`。

## 花店草地背景（当前任务映射）

- 提示词：`docs/prompt/house_yard_bg_nb2_prompt.txt`
- 原图：`../game_assets/huahua/assets/raw/house_yard_bg_nb2.png`（自仓库根）
- 游戏内：`minigame/subpkg_deco/images/house/bg.png`（`TextureCache.house_bg`）

## 花店房间壳 bg_room_* 试看图

- 先试做输出到：`../game_assets/huahua/assets/preview_house_room/`（自仓库根）
- 仓库对比副本：`minigame/subpkg_deco/images/house/preview/`（不接入 TextureCache）
- 示例：`house_bg_room_default_v2_nb2_prompt.txt` → `bg_room_default_v2_nb2.png`
