One UI sprite sheet, flat layout, exactly four columns and two rows, eight equal rectangular cells with even spacing. No scene, no frame around the whole sheet beyond the cells themselves.

Each cell contains exactly one horizontal pill-shaped button (stadium capsule: fully rounded ends). Proportions must match the attached reference: a **strongly flattened** capsule—**much wider than tall** (low profile, 扁), like a thin horizontal lozenge, **not** a tall skinny chip and **not** a vertically stretched pill. The pill’s **length-to-height ratio** should closely follow the reference image. Do **not** stretch the button across the full width of the cell: use only about **55% to 65% of the cell width**, centered, with generous white margin on left and right so the shape never looks “too long” in the grid. Every button uses the same proportions, outline thickness, and lighting recipe so they look like one asset family.

Visual recipe for every button (match the attached reference for capsule shape, stroke, and lighting only): warm dark brown outer stroke; pastel glossy fill; subtle darker inner band along bottom and right inside edge for soft 3D; small soft lighter oval highlight near the top-left. The button face must be completely empty: no checkmark, no tick, no icons, no symbols, no letters, no numbers, no emoji—only smooth pastel fill inside the outline. Clean 2D vector kawaii merge-game UI, pastel high key, no photoreal, no watermark.

Assign one distinct pastel fill per cell, coordinated for a cozy flower-shop merge game (lilac, pink, cream, mint family). Suggested order left-to-right, top then bottom: (1) soft mint lime green like the reference, (2) pastel sky blue, (3) soft butter yellow, (4) soft lavender purple, (5) gentle peach coral, (6) pastel pink, (7) soft aqua teal, (8) warm cream with a hint of honey gold. Do not repeat the same hex twice.

Background: pure flat white RGB 255,255,255 filling the entire canvas outside and between the eight buttons so the sheet is easy to separate. Keep the grid visually clean; if you draw separators, use hairline light gray only, or none.

Output must read as eight separate buttons on one 4x2 grid, centered in their cells, identical art style across all eight.
