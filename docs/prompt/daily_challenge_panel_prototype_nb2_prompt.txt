Full-screen vertical mobile game UI prototype, 9:16 aspect, high polish 2D illustrated casual merge-and-flower-shop game panel. Match the cozy art direction of a florist merge title: warm cream and blush pink, soft mint accents, light gold trim, desaturated plum or deep mauve wood grain for outer frame — same premium cute vector look as in-game shop panels (rounded shapes, soft drop shadows, gentle gradients for depth only).

OUTER CHROME: large centered modal with thick rounded-rectangle wooden border, subtle white dashed stitch detail along the outer rim, inner soft lavender-pink highlight lip. Top-right: circular red close button with a simple white X glyph (decorative, not a logo).

HEADER (compact, about 18% to 22% of frame height): whimsical title zone with empty decorative yellow-gold bubble plaque (no readable title text). Below it, a small stopwatch icon beside a wide empty pill for a countdown timer (no digits). Under that, a short empty subtitle ribbon. Then a slim horizontal decorative bar shaped like a progress capsule in soft sky blue with rounded ends (empty center, no text) suggesting “time until refresh.”

MAIN BODY (middle 55% to 60%): vertical stack of five rounded light-beige task cards with generous padding and soft shadow. Each card shows: left side large mint-green checkmark badge for completed state on the first three cards, empty circle placeholder on lower cards; center wide empty lines for future Chinese task copy; right side a small golden hex coin icon beside a blank reward value pill; far right a tiny cute reward prop icon (stamina leaf sparkles, small treasure chest, diamond bag) as generic game loot silhouettes. Cards should feel like tiered goals of one activity family (ascending difficulty vibe) without any readable words.

BOTTOM WEEKLY STRIP (about 18% to 22% height): bold horizontal progress track with warm yellow-gold fill stopping around two-thirds across; neutral taupe track. Along the bar, six small milestone markers as gold studs or tiny gem pins (no numbers, no letters). Above each marker, a tiny floating reward icon (coin stack, gift box, chest) with green check sticker on the left milestones only. Bookend the bar with cute round yellow mascot faces with small red bows (simple emoji-like, original design). Below the bar, a tiny empty caption strip for future timer text.

INTERIOR BACKDROP behind cards: soft dreamy illustration — light blue to lavender vertical gradient with faint stylized snow-dusted pine silhouettes OR soft cherry-blossom tree silhouettes (pick one cohesive winter-cozy mood that still feels floral-game friendly), very low contrast so UI stays readable.

Art style: clean mobile game UI illustration, not photoreal, no watermark, no brand logos, no readable text or numerals anywhere in any language (use blanks, empty pills, abstract ticks only).
