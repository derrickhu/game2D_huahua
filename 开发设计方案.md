《花语小筑》Phaser 3 开发设计方案

一、技术选型总览

1. 核心技术栈

• 游戏引擎：Phaser 3.80+（最新稳定版）
• 开发语言：TypeScript 5.x（强类型，团队协作必备）
• 构建工具：Vite 5.x（极速 HMR，Tree Shaking，微信小游戏打包友好）
• UI 插件：rexrainbow/phaser3-rex-plugins（ScrollablePanel、GridSizer、Dialog 等）
• 图集工具：TexturePacker / Free Texture Packer（图集合并，减少 DrawCall）
• 代码规范：ESLint + Prettier
• 版本管理：Git

2. 目标平台

• 主平台：微信小游戏
• 适配方案：weapp-adapter（桥接 Canvas/Audio/Touch/Storage API）
• 设计分辨率：750×1334（竖屏，iPhone 6/7/8 基准）
• 缩放模式：Phaser.Scale.FIT（等比缩放适配各种屏幕）
• 渲染模式：优先 WebGL，降级 Canvas

3. 运行环境约束

• 微信小游戏包体限制：主包 ≤ 4MB，分包 ≤ 2MB/个，总计 ≤ 20MB
• 远程 CDN 资源：大部分图集和音效走 CDN 按需下载
• 本地存储：wx.setStorageSync / wx.getStorageSync（存档数据）
• 网络通信：wx.request（排行榜、社交、活动数据）

二、项目工程结构

```
huahua/
├── public/                          # 静态资源（开发环境直接访问）
│   └── index.html
├── src/
│   ├── main.ts                      # 入口文件，初始化 Phaser.Game
│   ├── config/
│   │   ├── GameConfig.ts            # Phaser.GameConfig 配置
│   │   ├── Constants.ts             # 全局常量（格子尺寸、CD时间、概率表等）
│   │   └── AssetKeys.ts            # 资源 Key 枚举（避免硬编码字符串）
│   ├── scenes/
│   │   ├── BootScene.ts            # 启动场景：加载 Loading 资源
│   │   ├── PreloadScene.ts         # 预加载场景：加载所有游戏资源
│   │   ├── MainScene.ts            # 合成经营主场景（核心）
│   │   ├── DecorationScene.ts      # 花店装修场景
│   │   ├── DressUpScene.ts         # 店主换装场景
│   │   ├── AlbumScene.ts           # 图鉴场景
│   │   ├── SocialScene.ts          # 社交场景
│   │   ├── UIScene.ts              # 常驻 UI 场景（顶部栏+底部导航栏）
│   │   └── OpenShopScene.ts        # 开店仪式场景（叠加展示）
│   ├── gameobjects/
│   │   ├── board/
│   │   │   ├── Board.ts            # 棋盘管理器（网格逻辑）
│   │   │   ├── Cell.ts             # 单个格子
│   │   │   ├── BoardItem.ts        # 通用物品（花束+花饮双品类，可拖拽，锁定仍可合成）
│   │   │   ├── FlowerItem.ts       # [已废弃] 旧版花朵物品，由 BoardItem 替代
│   │   │   └── Building.ts         # 生产建筑（花束建筑+饮品建筑，支持产出线选择弹窗）
│   │   ├── customer/
│   │   │   ├── Customer.ts         # 客人基类
│   │   │   ├── RegularCustomer.ts  # 熟客（含好感度）
│   │   │   └── CustomerBubble.ts   # 客人需求气泡
│   │   ├── shop/
│   │   │   ├── ShopOwner.ts        # 店主形象（Q版+立绘切换）
│   │   │   ├── Counter.ts          # 柜台
│   │   │   └── ShopBackground.ts   # 花店背景（装修联动）
│   │   ├── decoration/
│   │   │   ├── DecoSlot.ts         # 装饰槽位
│   │   │   └── DecoItem.ts         # 单个装饰选项
│   │   └── dressup/
│   │       ├── AvatarRenderer.ts   # 换装立绘渲染器（多层叠加）
│   │       └── ClothingItem.ts     # 服装选项卡片
│   ├── managers/
│   │   ├── GameDataManager.ts      # 全局数据管理（单例）
│   │   ├── CurrencyManager.ts      # 货币系统（金币/花愿/花露/钻石/活动花瓣）
│   │   ├── BoardManager.ts         # 棋盘逻辑管理
│   │   ├── MergeManager.ts         # 合成逻辑管理
│   │   ├── CustomerManager.ts      # 客人刷新/交付/熟客好感度
│   │   ├── OrderManager.ts         # 订单管理（即时/每日/限时/熟客）
│   │   ├── BuildingManager.ts      # 建筑 CD/产出/花系选择
│   │   ├── DecorationManager.ts    # 装修槽位/解锁/切换
│   │   ├── DressUpManager.ts       # 换装部件/解锁/穿戴
│   │   ├── AlbumManager.ts         # 图鉴收集/奖励
│   │   ├── TaskManager.ts          # 任务系统（主线/每日/成就）
│   │   ├── IdleManager.ts          # 挂机/离线收益计算
│   │   ├── AdManager.ts            # 广告管理（微信激励视频）
│   │   ├── SocialManager.ts        # 社交功能（互访/助力/排行榜）
│   │   ├── GuideManager.ts         # 新手引导状态机
│   │   ├── AudioManager.ts         # 音效/BGM 管理
│   │   ├── SaveManager.ts          # 存档管理（本地+云端）
│   │   └── EventManager.ts         # 全局事件总线（EventEmitter）
│   ├── data/
│   │   ├── FlowerData.ts           # 花朵配置（3花系×6级，名称/图标/售价）
│   │   ├── DrinkData.ts            # 花饮配置（3饮品线×3级，名称/售价）
│   │   ├── ItemData.ts             # 统一物品接口（桥接FlowerData和DrinkData）
│   │   ├── BuildingData.ts         # 建筑配置（7种：4花束+3饮品，概率产出表/解锁价格）
│   │   ├── CustomerData.ts         # 客人配置（16种客人，组合订单1~3个需求槽位）
│   │   ├── RegularCustomerData.ts  # 熟客配置（8位，好感度奖励表）
│   │   ├── DecorationData.ts       # 装修配置（10槽位，每槽位装饰列表）
│   │   ├── ClothingData.ts         # 服装配置（分类/稀有度/花露价格）
│   │   ├── TaskData.ts             # 任务配置
│   │   ├── OrderData.ts            # 订单配置
│   │   └── ActivityData.ts         # 限时活动配置
│   ├── ui/
│   │   ├── components/
│   │   │   ├── Button.ts           # 通用按钮（带音效+缩放动画）
│   │   │   ├── CurrencyBar.ts      # 货币显示条
│   │   │   ├── ProgressBar.ts      # 进度条（好感度/CD/收集度）
│   │   │   ├── Dialog.ts           # 通用弹窗
│   │   │   ├── Toast.ts            # 轻提示（棋盘已满/合成失败等）
│   │   │   ├── RedDot.ts           # 红点组件
│   │   │   ├── ScrollList.ts       # 可滚动列表（基于 rex ScrollablePanel）
│   │   │   ├── GridPanel.ts        # 网格面板（图鉴/服装列表）
│   │   │   ├── TabBar.ts           # 标签栏组件
│   │   │   └── CardItem.ts         # 卡片组件（图鉴/装饰/服装通用）
│   │   ├── NavBar.ts               # 底部导航栏（5个按钮）
│   │   ├── TopBar.ts               # 顶部信息栏
│   │   ├── OrderPanel.ts           # 每日订单面板
│   │   ├── TaskPanel.ts            # 任务面板
│   │   ├── SettlementPanel.ts      # 日结算面板
│   │   └── ShareCard.ts            # 分享卡片生成器
│   ├── effects/
│   │   ├── MergeEffect.ts          # 合成特效（闪光+粒子）
│   │   ├── DeliverEffect.ts        # 交付特效（花朵飞向客人+金币飘出）
│   │   ├── BuildingEffect.ts       # 建筑产出特效
│   │   ├── UnlockEffect.ts         # 解锁特效（建筑/装修/服装通用）
│   │   └── CurrencyFlyEffect.ts    # 货币飞入顶栏特效
│   ├── wx/
│   │   ├── WxAdapter.ts            # 微信 API 适配层
│   │   ├── WxAd.ts                 # 微信激励视频/Banner 广告
│   │   ├── WxShare.ts              # 微信分享
│   │   ├── WxStorage.ts            # 微信本地存储封装
│   │   └── WxSocial.ts             # 微信关系链/开放数据域
│   └── utils/
│       ├── MathUtils.ts            # 数学工具（概率计算、随机选择）
│       ├── TimeUtils.ts            # 时间工具（离线时长计算、CD 格式化）
│       ├── ArrayUtils.ts           # 数组工具（洗牌、查找空格等）
│       └── Logger.ts               # 日志工具（开发环境打印，生产环境静默）
├── assets/
│   ├── atlas/                      # 图集（TexturePacker 输出）
│   │   ├── flowers.json + .png     # 18种花朵图集
│   │   ├── buildings.json + .png   # 12种建筑图集
│   │   ├── customers.json + .png   # 16种客人图集
│   │   ├── ui.json + .png          # UI 元素图集
│   │   ├── decorations.json + .png # 装修装饰图集
│   │   └── effects.json + .png     # 特效帧图集
│   ├── spine/                      # Spine 动画（店主角色，可选）
│   ├── audio/
│   │   ├── bgm/                    # 背景音乐
│   │   └── sfx/                    # 音效
│   ├── dressup/                    # 换装资源（按部件分文件夹，CDN 按需加载）
│   │   ├── hair/
│   │   ├── top/
│   │   ├── bottom/
│   │   ├── shoes/
│   │   ├── accessory/
│   │   └── set/
│   └── story/                      # 熟客故事文本+插图
├── typings/
│   └── wx.d.ts                     # 微信小游戏 TypeScript 类型定义
├── vite.config.ts
├── tsconfig.json
├── package.json
└── project.config.json             # 微信小游戏项目配置
```

三、Phaser Game 初始化配置

```typescript
// src/config/GameConfig.ts

import Phaser from 'phaser';
import { BootScene } from '../scenes/BootScene';
import { PreloadScene } from '../scenes/PreloadScene';
import { MainScene } from '../scenes/MainScene';
import { DecorationScene } from '../scenes/DecorationScene';
import { DressUpScene } from '../scenes/DressUpScene';
import { AlbumScene } from '../scenes/AlbumScene';
import { SocialScene } from '../scenes/SocialScene';
import { UIScene } from '../scenes/UIScene';
import { OpenShopScene } from '../scenes/OpenShopScene';

export const gameConfig: Phaser.Types.Core.GameConfig = {
  type: Phaser.AUTO,              // 优先 WebGL，自动降级 Canvas
  width: 750,
  height: 1334,
  parent: 'game-container',
  backgroundColor: '#FFF8F0',     // 温暖米白底色
  scale: {
    mode: Phaser.Scale.FIT,       // 等比缩放
    autoCenter: Phaser.Scale.CENTER_BOTH,
  },
  input: {
    activePointers: 2,            // 支持双指操作（换装页缩放）
  },
  scene: [
    BootScene,
    PreloadScene,
    MainScene,
    DecorationScene,
    DressUpScene,
    AlbumScene,
    SocialScene,
    UIScene,
    OpenShopScene,
  ],
  physics: {
    default: 'arcade',            // 轻量物理（仅用于花朵飘落动画等）
    arcade: {
      gravity: { x: 0, y: 0 },
      debug: false,
    },
  },
  render: {
    pixelArt: false,              // 非像素风，使用抗锯齿
    antialias: true,
    roundPixels: true,
  },
};
```

```typescript
// src/main.ts

import Phaser from 'phaser';
import { gameConfig } from './config/GameConfig';

const game = new Phaser.Game(gameConfig);
```

四、Scene 架构设计（核心中的核心）

1. Scene 生命周期与管理策略

```
游戏启动流程：
BootScene.init()
  → 加载 loading 进度条所需的最小资源
  → 启动 PreloadScene

PreloadScene.preload()
  → 加载所有游戏核心资源（图集/音效/配置）
  → 显示加载进度条
  → 加载完成后：
    → 启动 UIScene（常驻，永不销毁）
    → 判断是否首次进入：
      → 首次：启动 MainScene + GuideManager
      → 非首次：启动 OpenShopScene（开店仪式）→ 仪式结束后启动 MainScene

运行时 Scene 管理：
┌─────────────────────────────────────────┐
│  UIScene（常驻运行，depth 最高）          │
│  ├── TopBar（顶部信息栏）                │
│  └── NavBar（底部导航栏）                │
├─────────────────────────────────────────┤
│  当前活跃 Scene（同一时间只有一个活跃）    │
│  ├── MainScene（合成经营）               │
│  ├── DecorationScene（装修）             │
│  ├── DressUpScene（换装）                │
│  ├── AlbumScene（图鉴）                  │
│  └── SocialScene（社交）                 │
├─────────────────────────────────────────┤
│  叠加 Scene（按需弹出）                  │
│  ├── OpenShopScene（开店仪式）           │
│  └── 各种 Dialog/Panel                  │
└─────────────────────────────────────────┘
```

2. Scene 切换逻辑

```typescript
// UIScene 中处理导航切换
// NavBar 点击事件 → 切换到目标 Scene

class UIScene extends Phaser.Scene {
  private currentSceneKey: string = 'MainScene';

  switchScene(targetKey: string): void {
    if (targetKey === this.currentSceneKey) return;

    // 休眠当前场景（保持状态，不销毁）
    this.scene.sleep(this.currentSceneKey);

    // 唤醒或启动目标场景
    if (this.scene.isSleeping(targetKey)) {
      this.scene.wake(targetKey);
    } else {
      this.scene.launch(targetKey);
    }

    // 将 UIScene 置于最顶层
    this.scene.bringToTop('UIScene');

    this.currentSceneKey = targetKey;
  }
}
```

关键设计原则：
• UIScene 始终运行在最顶层，所有页面共享顶部栏和底部导航栏
• 页面切换使用 sleep/wake 而非 stop/start，保持各 Scene 的内部状态
• MainScene 被 sleep 时，内部定时器（建筑CD/客人刷新/挂机产出）继续运行（Phaser TimerEvent 在 sleep 时暂停，需使用 this.time 的 paused 管理或在 wake 时重新计算差值）

3. MainScene 内部结构

```typescript
class MainScene extends Phaser.Scene {
  // 区域容器
  private shopArea: Phaser.GameObjects.Container;    // 区域B：花店场景
  private boardArea: Phaser.GameObjects.Container;    // 区域C：合成棋盘

  // 核心管理器（MainScene 专属）
  private board: Board;
  private customerManager: CustomerManager;
  private buildingManager: BuildingManager;
  private mergeManager: MergeManager;
  private idleManager: IdleManager;
  private guideManager: GuideManager;

  // 核心显示对象
  private shopOwner: ShopOwner;        // 店主Q版形象
  private counter: Counter;            // 柜台
  private shopBg: ShopBackground;      // 花店背景

  create(): void {
    this.createShopArea();
    this.createBoardArea();
    this.initManagers();
    this.setupDragAndDrop();
    this.startGameLoop();
  }

  // sleep 时暂停非关键更新，记录时间戳
  // wake 时计算差值，补发离线产出
}
```

五、核心系统详细设计

===== 系统1：棋盘与合成系统（双品类） =====

1. 数据模型

```typescript
// 商品品类
enum ItemCategory {
  FLOWER = 'flower',  // 花束
  DRINK = 'drink',    // 花饮
}

// 花系（花束品类下的子分类）
enum FlowerFamily {
  DAILY = 'daily',       // 日常花系
  ROMANTIC = 'romantic',  // 浪漫花系
  LUXURY = 'luxury',      // 奢华花系
}

// 饮品线（花饮品类下的子分类）
enum DrinkLine {
  TEA = 'tea',        // 茶饮线
  COLD = 'cold',      // 冷饮线
  DESSERT = 'dessert', // 甜品线
}

// 统一物品接口（同时描述花束和花饮）
interface ItemInfo {
  id: string;
  category: ItemCategory;
  line: string;        // FlowerFamily 或 DrinkLine
  level: number;
  name: string;
  sellPrice: number;
  color: number;
  maxLevel: number;    // 花束6级，花饮3级
}

// 花朵数据（3花系×6级 = 18种）
interface FlowerConfig {
  id: string;             // 如 'daily_1', 'romantic_3'
  family: FlowerFamily;
  level: number;          // 1~6
  name: string;
  sellPrice: number;
  color: number;
}

// 花饮数据（3饮品线×3级 = 9种）
interface DrinkConfig {
  id: string;             // 如 'tea_1', 'cold_2'
  line: DrinkLine;
  level: number;          // 1~3
  name: string;
  sellPrice: number;
  color: number;
}

// 格子状态
interface CellState {
  row: number;
  col: number;
  content: 'empty' | 'item' | 'building' | 'locked';
  itemId?: string;        // 统一物品ID
  buildingId?: string;
}
```

合成链长度说明：
- 花束：6级长链，需要32个基础物品合成满级
- 花饮：3级短链，需要4个基础物品合成满级

2. BoardItem 类设计（替代旧版 FlowerItem）

```typescript
// 棋盘上的通用物品（花束 or 花饮）
class BoardItem extends Phaser.GameObjects.Container {
  public itemId: string;
  public category: ItemCategory;
  public line: string;         // FlowerFamily 或 DrinkLine
  public level: number;
  public isReserved: boolean;  // 被客人锁定
  public reservedBySlot: number;   // 被哪个客人槽位锁定
  public reservedDemandIdx: number; // 被锁定到客人的第几个需求槽位

  // 核心策略点：锁定物品仍可拖拽和合成！
  // setReserved() 只显示金色边框和对勾标记，不禁止交互
  setReserved(reserved: boolean, slotIndex?: number, demandIdx?: number): void;

  // 视觉区分：花束=圆形，花饮=圆角方形
  private drawItem(): void;

  // 兼容旧接口
  get flowerId(): string;  // → this.itemId
  get family(): string;    // → this.line
}
```

3. Board 合成逻辑

```typescript
class MainScene {
  // 合成规则：同品类+同线+同等级+未满级
  // 关键改变：锁定物品(isReserved)仍可合成！
  private canMerge(a: BoardItem, b: BoardItem): boolean {
    return a.category === b.category
      && a.line === b.line
      && a.level === b.level
      && a.level < a.maxLevel  // 花束<6，花饮<3
      && a !== b;
  }

  private doMerge(source: BoardItem, target: BoardItem, targetCell: Cell): void {
    // 如果被合成的物品是锁定的，通知客人系统解锁对应槽位
    if (source.isReserved) EventManager.emit(GameEvents.ITEM_CONSUMED, { item: source });
    if (target.isReserved) EventManager.emit(GameEvents.ITEM_CONSUMED, { item: target });

    // 销毁源和目标，放置新的高级物品
    const nextId = getNextLevelId(source.itemId);
    // ...合成特效+放置新物品
  }
}
```

===== 系统2：建筑产出系统（双品类，无CD） =====

```typescript
// 建筑配置数据
interface BuildingConfig {
  id: string;
  name: string;
  category: ItemCategory;              // 花束建筑 or 饮品建筑
  unlockPrice: number;                 // 金币解锁价格
  unlockOrder: number;                 // 解锁顺序（1~15）
  selectableLines: string[];           // 可选产出线（FlowerFamily[] 或 DrinkLine[]）
  outputTable: OutputEntry[];          // 产出概率表
}

interface OutputEntry {
  levelRange: [number, number];  // [min, max] 等级范围
  weight: number;                // 权重
}

// 7种建筑（4花束+3饮品）
// 花束建筑：花艺操作台、包装台、小型温室、星光花房
// 饮品建筑：简易茶台、调饮吧台、花饮工坊

class Building extends Phaser.GameObjects.Container {
  private config: BuildingConfig;

  // 关键改变：无CD！点击即产出（与四季物语对齐）
  onTap(): void {
    if (this.config.selectableLines.length <= 1) {
      // 初级建筑，无需选择，直接产出
      this.produce(this.config.selectableLines[0]);
    } else {
      // 中高级建筑，弹出产出线选择浮层
      this.showLineSelector();
    }
  }

  // 产出线选择浮层
  private showLineSelector(): void {
    // 浮动面板，每个产出线一个带颜色的按钮
    // 花束建筑显示花系选择（日常/浪漫/奢华）
    // 饮品建筑显示饮品线选择（茶饮/冷饮/甜品）
    // 点击外部自动关闭
  }

  // 产出逻辑
  produce(line: string): void {
    const level = rollOutputLevel(this.config.outputTable);
    const itemId = `${line}_${level}`;

    EventManager.emit(GameEvents.BUILDING_PRODUCED, {
      buildingId: this.buildingId,
      itemId,
      row: this.row,
      col: this.col,
    });
    // Board 监听此事件，在建筑相邻空格放置新物品
  }

  // 视觉区分：花束建筑=棕色+三角屋顶，饮品建筑=蓝灰色+杯子造型
  private drawBuilding(): void;
}
```

===== 系统3：客人与组合订单系统（自动锁定+手动确认） =====

```typescript
// 单个需求槽位
interface DemandSlot {
  category: ItemCategory;  // 花束 or 花饮
  line: string;            // FlowerFamily 或 DrinkLine
  minLevel: number;
  maxLevel: number;
}

// 订单模板（1~3个需求槽位的组合订单）
interface OrderTemplate {
  demands: DemandSlot[];
  goldReward: number;
  wishReward: number;
  dewReward: number;
}

// 需求槽位运行时状态
interface DemandSlotState {
  demand: DemandSlot;
  locked: boolean;           // 是否已被棋盘物品锁定
  lockedItem: BoardItem | null;  // 锁定的物品引用
}

// 客人配置（16种客人）
interface CustomerConfig {
  id: string;
  name: string;
  color: number;
  isRegular: boolean;       // 是否为可养成熟客
  possibleOrders: OrderTemplate[];
}

class CustomerManager {
  private maxCustomers: number = 2;
  private activeCustomers: Customer[] = [];
  private drinkUnlocked: boolean = false;

  // 核心机制：自动扫描棋盘，为客人需求槽位匹配物品
  // 每500ms扫描一次 + 物品变化时立即扫描
  scanAndReserve(): void {
    for (const customer of this.activeCustomers) {
      for (const slot of customer.demandStates) {
        if (slot.locked) {
          // 检查已锁定物品是否还有效
          if (!slot.lockedItem?.scene) {
            customer.unlockDemandSlot(slotIdx);
          }
          continue;
        }
        // 寻找匹配的未锁定物品，优先选等级最低的
        const match = this.findBestMatch(slot.demand);
        if (match) {
          match.setReserved(true, customer.slotIndex, slotIdx);
          customer.lockDemandSlot(slotIdx, match);
        }
      }
    }
  }

  // 关键策略改变：
  // 1. 锁定物品仍可拖拽和合成！
  // 2. 如果锁定物品被合成消耗，对应需求槽位自动解锁，系统重新扫描
  // 3. 全部需求槽位满足后显示"完成"按钮，玩家手动确认交付
  // 4. 取消旧的拖拽交付方式

  // 监听事件
  // ITEM_CONSUMED → 解锁被消耗物品的槽位 → 重新扫描
  // ITEM_MERGED/ITEM_PLACED → 重新扫描
  // BUILDING_PRODUCED → 延迟100ms后重新扫描

  // 游戏进度控制
  // drinkUnlocked=false时：只出现纯花束需求的客人和订单
  // drinkUnlocked=true时：出现全部16种客人，含花+饮组合订单
}

class Customer extends Phaser.GameObjects.Container {
  public demandStates: DemandSlotState[];

  // 多槽位气泡UI：每个槽位显示品类图标+等级需求+颜色边框
  // 槽位满足后显示绿色对勾覆盖
  // 全部满足后弹出"完成"按钮（带弹跳吸引动画）

  // 等待时间随槽位数增加：base + (slotCount-1) * 20s

  lockDemandSlot(idx: number, item: BoardItem): void;
  unlockDemandSlot(idx: number): void;
  unlockAllSlots(): void;
  isAllSatisfied(): boolean;
  getLockedItems(): BoardItem[];

  destroy(): void {
    // 销毁时解除所有物品锁定
  }
}
```

===== 系统4：装修系统 =====

```typescript
// 装饰槽位管理
class DecorationManager {
  private slots: Map<string, DecoSlot> = new Map();

  // 10个槽位
  private static SLOT_KEYS = [
    'facade', 'signboard', 'window', 'courtyard', 'floor',
    'wall', 'chandelier', 'counter', 'shelf', 'lounge'
  ];

  // 初始化槽位
  init(): void {
    for (const key of DecorationManager.SLOT_KEYS) {
      const config = DecorationData.getSlotConfig(key);
      const slot = new DecoSlot(key, config);
      this.slots.set(key, slot);
    }
  }

  // 切换装饰
  equipDecoration(slotKey: string, decoId: string): void {
    const slot = this.slots.get(slotKey);
    if (!slot) return;

    slot.equip(decoId);

    // 更新主页面花店背景
    EventManager.emit('decoration:changed', { slotKey, decoId });

    // 重算美观度
    this.recalcBeautyScore();
  }

  // 解锁装饰
  unlockDecoration(slotKey: string, decoId: string): boolean {
    const cost = DecorationData.getDecoPrice(slotKey, decoId);
    if (!CurrencyManager.hasEnoughWish(cost)) return false;

    CurrencyManager.spendWish(cost);
    const slot = this.slots.get(slotKey);
    slot.unlock(decoId);
    return true;
  }

  // 美观度评分（用于排行榜）
  recalcBeautyScore(): number {
    let score = 0;
    for (const slot of this.slots.values()) {
      score += slot.getEquippedScore();
    }
    return score;
  }
}

// 装修 Scene 中的交互
class DecorationScene extends Phaser.Scene {
  private shopView: Phaser.GameObjects.Container;  // 花店45°全景
  private slotHighlights: Map<string, Phaser.GameObjects.Graphics>;
  private selectionPanel: ScrollList;  // 底部装饰选择面板

  create(): void {
    this.createShopPanorama();
    this.createSlotInteractions();
    this.createSelectionPanel();
  }

  // 点击槽位
  private onSlotTap(slotKey: string): void {
    // 高亮选中槽位
    this.highlightSlot(slotKey);

    // 底部面板展示该槽位的装饰列表
    const items = DecorationData.getSlotItems(slotKey);
    this.selectionPanel.setItems(items);
    this.selectionPanel.slidein();  // 从底部滑入
  }

  // 选择装饰 → 实时预览
  private onDecoSelect(slotKey: string, decoId: string): void {
    // 临时替换显示（预览，未确认）
    this.shopView.previewDecoration(slotKey, decoId);
  }

  // 确认装备
  private onDecoConfirm(slotKey: string, decoId: string): void {
    DecorationManager.equipDecoration(slotKey, decoId);
    UnlockEffect.play(this.scene, /* ... */);
  }
}
```

===== 系统5：换装系统 =====

```typescript
// 换装部件分层渲染
class AvatarRenderer extends Phaser.GameObjects.Container {
  // 从底到顶的渲染层级
  private layers: Map<string, Phaser.GameObjects.Sprite> = new Map();

  private static LAYER_ORDER = [
    'body',       // 身体底层（固定）
    'bottom',     // 下装
    'shoes',      // 鞋子
    'top',        // 上装
    'hair_back',  // 后发
    'accessory',  // 饰品（包包等）
    'hair_front', // 前发
    'earring',    // 耳环
    'hair_deco',  // 发饰
    'special',    // 特殊装饰（翅膀/光环）
  ];

  constructor(scene: Phaser.Scene, x: number, y: number) {
    super(scene, x, y);
    this.initLayers();
  }

  // 切换某个部件
  setPartTexture(partKey: string, textureKey: string, frame?: string): void {
    const sprite = this.layers.get(partKey);
    if (sprite) {
      sprite.setTexture(textureKey, frame);
    }
  }

  // 穿套装 → 同时替换多个部件
  wearSet(setConfig: ClothingSetConfig): void {
    for (const [partKey, textureInfo] of Object.entries(setConfig.parts)) {
      this.setPartTexture(partKey, textureInfo.key, textureInfo.frame);
    }
  }

  // 旋转预览（正/侧/背）
  setAngle(angle: 'front' | 'side' | 'back'): void {
    for (const [partKey, sprite] of this.layers) {
      const frameSuffix = `_${angle}`;
      sprite.setFrame(sprite.frame.name.replace(/_\w+$/, frameSuffix));
    }
  }
}

// 换装 Scene
class DressUpScene extends Phaser.Scene {
  private avatar: AvatarRenderer;       // 左侧立绘
  private categoryTabs: TabBar;          // 分类标签
  private clothingGrid: GridPanel;       // 服装网格
  private currentTryOn: Map<string, string> = new Map(); // 当前试穿状态

  create(): void {
    // 左侧立绘区（45%宽度）
    this.avatar = new AvatarRenderer(this, 170, 500);
    this.avatar.loadCurrentOutfit();

    // 右侧分类标签
    this.categoryTabs = new TabBar(this, 380, 100, {
      tabs: ['发型','上装','下装','套装','鞋子','发饰','耳环','包包','特殊'],
      onSelect: (index) => this.onCategoryChange(index),
    });

    // 服装网格列表（可滚动）
    this.clothingGrid = new GridPanel(this, 380, 200, {
      cols: 2,
      cellWidth: 160,
      cellHeight: 200,
      onItemTap: (itemId) => this.onClothingTap(itemId),
      onItemLongPress: (itemId) => this.onClothingLongPress(itemId),
    });

    // 底部按钮
    this.createActionButtons();

    // 手势支持
    this.setupGestures();
  }

  private onClothingTap(itemId: string): void {
    const item = ClothingData.getItem(itemId);

    if (item.unlocked) {
      // 已解锁 → 试穿预览
      this.avatar.setPartTexture(item.partKey, item.textureKey, item.frame);
      this.currentTryOn.set(item.partKey, itemId);
    } else {
      // 未解锁 → 弹出解锁确认
      Dialog.show({
        title: item.name,
        content: `需要 ${item.price} 花露解锁`,
        confirmText: '解锁',
        onConfirm: () => this.tryUnlock(item),
      });
    }
  }

  // 确认穿戴
  private confirmOutfit(): void {
    for (const [partKey, itemId] of this.currentTryOn) {
      DressUpManager.equip(partKey, itemId);
    }
    // 通知主页面更新店主形象
    EventManager.emit('dressup:changed');
    Toast.show('搭配已更新~');
  }

  // 截图分享
  private captureAndShare(): void {
    // 隐藏UI，只保留角色+背景
    this.categoryTabs.setVisible(false);
    this.clothingGrid.setVisible(false);

    this.game.renderer.snapshot((image: HTMLImageElement) => {
      WxShare.shareImage(image, '来看看我的花店店主新造型~');
      this.categoryTabs.setVisible(true);
      this.clothingGrid.setVisible(true);
    });
  }

  // 手势：滑动旋转、双指缩放
  private setupGestures(): void {
    const rexGestures = this.rexUI.add.gestures(this.avatar);
    rexGestures.on('swipe', (swipe) => {
      if (swipe.direction === 'left') this.avatar.setAngle('side');
      if (swipe.direction === 'right') this.avatar.setAngle('front');
    });
    rexGestures.on('pinch', (pinch) => {
      const newScale = Phaser.Math.Clamp(this.avatar.scaleX * pinch.scaleFactor, 0.8, 1.5);
      this.avatar.setScale(newScale);
    });
  }
}
```

===== 系统6：挂机与离线收益（双品类） =====

```typescript
class MainScene {
  // 在线挂机产出（双品类）
  // 每60秒遍历所有建筑，每个建筑自动产出一个物品
  // 花束建筑产出花束，饮品建筑产出花饮
  // 挂机时默认使用建筑的第一条产出线
  private startIdleProduction(): void {
    this.time.addEvent({
      delay: IDLE.PRODUCE_INTERVAL, // 60000ms
      callback: () => {
        if (this.board.isFull()) return;
        const buildings = this.board.getAllBuildings();
        for (const { building } of buildings) {
          if (this.board.isFull()) break;
          const config = getBuildingConfig(building.buildingId);
          if (!config) continue;
          building.produce(config.selectableLines[0]);
        }
      },
      loop: true,
    });
  }

  // 离线收益计算
  // 最大离线时长：4小时
  // 离线金币 = 离线分钟数 × 建筑数量 × 2
  // 离线物品 = 每5分钟1个（最多10个），从已有建筑随机产出
  private processOfflineRewards(): void {
    const saveData = SaveManager.load();
    if (!saveData?.idle) return;

    const offlineMs = Math.min(Date.now() - saveData.idle.lastActiveTime, IDLE.MAX_OFFLINE_MS);
    if (offlineMs < 60000) return;

    const minutes = Math.floor(offlineMs / 60000);
    const buildings = this.board.getAllBuildings();
    const gold = minutes * Math.max(buildings.length, 1) * 2;
    const itemCount = Math.min(Math.floor(minutes / 5), 10);

    CurrencyManager.addGold(gold);
    // 放置离线产出物品...
    // 显示欢迎回来弹窗
  }
}
```

===== 系统7：存档系统 =====

```typescript
interface SaveData {
  version: number;
  timestamp: number;

  // 棋盘
  board: BoardState;

  // 货币
  currency: {
    gold: number;
    wish: number;     // 花愿
    dew: number;      // 花露
    diamond: number;
    eventPetal: number;
  };

  // 建筑
  buildings: {
    unlocked: string[];
    cdStates: { [buildingId: string]: { remaining: number; timestamp: number } };
  };

  // 客人/熟客
  customers: {
    servedTypes: string[];    // 已接待过的客人类型
    regularAffection: { [customerId: string]: number };  // 熟客好感度
    regularStoryUnlocked: { [customerId: string]: number[] }; // 已解锁故事章节
  };

  // 装修
  decoration: {
    unlocked: { [slotKey: string]: string[] };   // 每槽位已解锁装饰
    equipped: { [slotKey: string]: string };      // 每槽位当前装备
  };

  // 换装
  dressup: {
    unlocked: string[];                          // 已解锁服装ID列表
    equipped: { [partKey: string]: string };      // 每部件当前穿戴
  };

  // 图鉴
  album: {
    flowers: string[];      // 已发现的花朵ID
    customers: string[];    // 已接待的客人ID
    buildings: string[];    // 已解锁的建筑ID
    decorations: string[];  // 已解锁的装饰ID
    clothing: string[];     // 已解锁的服装ID
  };

  // 任务
  tasks: {
    mainProgress: number;
    dailyCompleted: string[];
    achievements: { [achievementId: string]: number };
  };

  // 每日数据（每日重置）
  daily: {
    date: string;           // YYYY-MM-DD
    ordersCompleted: number;
    goldEarned: number;
    customersServed: number;
    friendVisits: number;
    friendHelps: number;
    adWatched: { [adType: string]: number };
    goodRateHistory: number[];  // 最近3天好评率
  };

  // 挂机
  idle: {
    lastActiveTime: number;
  };

  // 新手引导
  guide: {
    step: number;
    completed: boolean;
  };
}

class SaveManager {
  private static SAVE_KEY = 'huahua_save';
  private static AUTO_SAVE_INTERVAL = 30000;  // 30秒自动存档

  // 保存（微信本地存储）
  static save(data: SaveData): void {
    data.timestamp = Date.now();
    const json = JSON.stringify(data);
    try {
      wx.setStorageSync(SaveManager.SAVE_KEY, json);
    } catch (e) {
      Logger.error('Save failed', e);
    }
  }

  // 读取
  static load(): SaveData | null {
    try {
      const json = wx.getStorageSync(SaveManager.SAVE_KEY);
      if (!json) return null;
      const data = JSON.parse(json) as SaveData;
      return this.migrate(data);  // 版本迁移
    } catch (e) {
      Logger.error('Load failed', e);
      return null;
    }
  }

  // 自动存档定时器
  static startAutoSave(scene: Phaser.Scene): void {
    scene.time.addEvent({
      delay: SaveManager.AUTO_SAVE_INTERVAL,
      callback: () => SaveManager.save(GameDataManager.serialize()),
      loop: true,
    });
  }

  // 版本迁移（处理存档格式更新）
  private static migrate(data: SaveData): SaveData {
    // 根据 data.version 逐步迁移到最新版本
    return data;
  }
}
```

===== 系统8：新手引导系统 =====

```typescript
class GuideManager {
  private currentStep: number = 0;
  private guideOverlay: Phaser.GameObjects.Graphics;  // 遮罩层
  private guideHand: Phaser.GameObjects.Sprite;       // 引导手指
  private guideText: Phaser.GameObjects.Text;          // 提示文本

  // 引导步骤定义
  private static STEPS: GuideStep[] = [
    // 阶段1：学会合成（0~2分钟）
    {
      id: 'merge_1',
      type: 'drag',
      description: '拖动这朵小雏菊到另一朵上试试~',
      highlightTarget: 'flower_daily_1_a',
      dragTarget: 'flower_daily_1_b',
      onComplete: 'spawn_two_sunflowers',
    },
    {
      id: 'merge_2',
      type: 'drag',
      description: '再合成一次，看看会变成什么花~',
      highlightTarget: 'flower_daily_2_a',
      dragTarget: 'flower_daily_2_b',
      onComplete: 'spawn_first_customer',
    },

    // 阶段2：交付客人（2~5分钟）
    {
      id: 'deliver_1',
      type: 'drag',
      description: '这位客人想要一束向日葵，拖给她吧~',
      highlightTarget: 'flower_daily_2',
      dragTarget: 'customer_bubble',
      onComplete: 'show_gold_tutorial',
    },

    // 阶段3：解锁建筑（5~10分钟）
    {
      id: 'unlock_building',
      type: 'tap',
      description: '用金币解锁花艺操作台~',
      highlightTarget: 'building_slot',
      onComplete: 'building_tap_tutorial',
    },
    {
      id: 'building_tap',
      type: 'tap',
      description: '点击花艺操作台，它会自动产花~',
      highlightTarget: 'building_1',
      onComplete: 'free_play_1',
    },

    // 阶段4：装修（10~20分钟，到达花愿阈值后触发）
    {
      id: 'decoration_intro',
      type: 'tap',
      description: '你的花店可以装修哦，去看看~',
      highlightTarget: 'nav_decoration',
      triggerCondition: 'wish >= 50',
    },
    {
      id: 'decoration_unlock',
      type: 'tap',
      description: '免费解锁第一件门面装饰~',
      highlightTarget: 'deco_facade_1',
    },

    // 阶段5：换装（20~30分钟，到达花露阈值后触发）
    {
      id: 'dressup_intro',
      type: 'tap',
      description: '给你的店主换套新衣服吧~',
      highlightTarget: 'nav_dressup',
      triggerCondition: 'dew >= 30',
    },
    {
      id: 'dressup_equip',
      type: 'tap',
      description: '点击这套清新裙装，免费获得~',
      highlightTarget: 'clothing_starter_set',
    },
    {
      id: 'guide_complete',
      type: 'dialog',
      description: '太棒了！你已经掌握了花店的所有技能，开始自由经营吧~',
    },
  ];

  // 执行当前步骤
  executeStep(step: GuideStep): void {
    // 1. 显示遮罩（除高亮目标外全部变暗）
    this.showMask(step.highlightTarget);

    // 2. 显示引导手指动画
    this.showGuideHand(step.highlightTarget, step.type);

    // 3. 显示提示文本
    this.showText(step.description);

    // 4. 限制输入，只允许操作引导目标
    this.restrictInput(step.highlightTarget, step.dragTarget);
  }

  // 步骤完成 → 下一步
  onStepComplete(): void {
    this.currentStep++;
    if (this.currentStep < GuideManager.STEPS.length) {
      const nextStep = GuideManager.STEPS[this.currentStep];
      if (nextStep.triggerCondition) {
        // 有触发条件，等待条件满足
        this.waitForCondition(nextStep.triggerCondition, () => {
          this.executeStep(nextStep);
        });
      } else {
        this.executeStep(nextStep);
      }
    } else {
      this.completeGuide();
    }
  }
}
```

===== 系统9：事件总线 =====

```typescript
// 全局事件总线（解耦各系统通信）
class EventManager {
  private static emitter = new Phaser.Events.EventEmitter();

  static on(event: string, callback: Function, context?: any): void {
    this.emitter.on(event, callback, context);
  }

  static emit(event: string, data?: any): void {
    this.emitter.emit(event, data);
  }

  static off(event: string, callback: Function, context?: any): void {
    this.emitter.off(event, callback, context);
  }
}

// 事件定义（统一管理，避免字符串硬编码）
const GameEvents = {
  // 合成（双品类）
  FLOWER_MERGED: 'flower:merged',     // 兼容旧代码
  FLOWER_PLACED: 'flower:placed',     // 兼容旧代码
  ITEM_MERGED: 'item:merged',         // 新：物品合成
  ITEM_PLACED: 'item:placed',         // 新：物品放置
  ITEM_CONSUMED: 'item:consumed',     // 新：物品被合成消耗（锁定物品被合并时触发）

  // 交付
  ORDER_COMPLETED: 'order:completed',
  CUSTOMER_ARRIVED: 'customer:arrived',
  CUSTOMER_LEFT: 'customer:left',

  // 熟客
  AFFECTION_UP: 'affection:up',
  AFFECTION_LEVEL_UP: 'affection:levelUp',
  STORY_UNLOCKED: 'story:unlocked',

  // 货币
  CURRENCY_CHANGED: 'currency:changed',

  // 建筑
  BUILDING_UNLOCKED: 'building:unlocked',
  BUILDING_PRODUCED: 'building:produced',
  BUILDING_CD_COMPLETE: 'building:cdComplete',

  // 装修
  DECORATION_CHANGED: 'decoration:changed',
  DECORATION_UNLOCKED: 'decoration:unlocked',

  // 换装
  DRESSUP_CHANGED: 'dressup:changed',
  DRESSUP_UNLOCKED: 'dressup:unlocked',

  // 图鉴
  ALBUM_UNLOCKED: 'album:unlocked',
  ALBUM_COMPLETE: 'album:complete',

  // 任务
  TASK_COMPLETED: 'task:completed',
  ACHIEVEMENT_UNLOCKED: 'achievement:unlocked',

  // 导航
  SCENE_SWITCH: 'scene:switch',

  // 引导
  GUIDE_STEP_COMPLETE: 'guide:stepComplete',
  GUIDE_COMPLETE: 'guide:complete',
} as const;
```

六、微信小游戏适配层

```typescript
// src/wx/WxAdapter.ts
// 微信小游戏环境适配（处理 Canvas/触控/音频 差异）

class WxAdapter {
  static init(): void {
    // 1. Canvas 适配
    // weapp-adapter 会自动创建 canvas 并挂载到 GameGlobal
    // Phaser 通过 config.canvas 传入

    // 2. 触控适配
    // 微信小游戏的 touch 事件已通过 adapter 桥接到 DOM 标准

    // 3. 性能监控
    wx.getPerformance();  // 获取性能数据
  }
}

// src/wx/WxAd.ts
class WxAd {
  private static rewardedAd: WechatMinigame.RewardedVideoAd | null = null;

  // 初始化激励视频广告
  static initRewardedAd(adUnitId: string): void {
    this.rewardedAd = wx.createRewardedVideoAd({ adUnitId });
    this.rewardedAd.onError((err) => Logger.error('Ad error', err));
  }

  // 展示激励视频
  static showRewardedAd(): Promise<boolean> {
    return new Promise((resolve) => {
      if (!this.rewardedAd) {
        resolve(false);
        return;
      }

      this.rewardedAd.onClose((res) => {
        resolve(res && res.isEnded);
      });

      this.rewardedAd.show().catch(() => {
        // 加载失败，重新加载后展示
        this.rewardedAd!.load().then(() => this.rewardedAd!.show());
      });
    });
  }
}

// src/wx/WxShare.ts
class WxShare {
  // 分享到微信
  static shareAppMessage(title: string, imageUrl: string): void {
    wx.shareAppMessage({
      title,
      imageUrl,
      query: `from=share&uid=${GameDataManager.getUid()}`,
    });
  }

  // 截图并分享
  static async shareScreenshot(scene: Phaser.Scene, title: string): Promise<void> {
    // 使用 canvas.toTempFilePath 生成临时图片
    const canvas = scene.game.canvas as any;
    const tempPath = await new Promise<string>((resolve) => {
      canvas.toTempFilePath({
        success: (res: any) => resolve(res.tempFilePath),
      });
    });
    this.shareAppMessage(title, tempPath);
  }
}

// src/wx/WxSocial.ts
class WxSocial {
  // 获取好友列表（开放数据域）
  static getFriendList(): Promise<FriendInfo[]> {
    return new Promise((resolve) => {
      wx.getFriendCloudStorage({
        keyList: ['beautyScore', 'albumProgress', 'weeklyRevenue'],
        success: (res) => resolve(res.data),
        fail: () => resolve([]),
      });
    });
  }

  // 上报数据到开放数据域（用于排行榜）
  static setUserCloudStorage(data: { key: string; value: string }[]): void {
    wx.setUserCloudStorage({ KVDataList: data });
  }
}
```

七、资源管理策略

1. 资源分包方案

```
主包（≤ 4MB）：
├── 引擎代码 + 业务代码（约 1.5MB gzip 后）
├── boot 加载所需最小资源（loading 条图、logo）
├── UI 基础图集（按钮、货币图标、导航栏）
└── 日常花系图集（最常用）

分包1 - 花店场景资源（≤ 2MB）：
├── 建筑图集
├── 客人图集
├── 花店背景基础版
└── 音效

分包2 - 装修资源（≤ 2MB）：
├── 装修装饰图集
├── 花店全景背景
└── 各槽位装饰选项图

分包3 - 换装资源（≤ 2MB）：
├── 店主立绘基础层
├── 初始服装图
└── 普通品质服装图

CDN 远程加载（按需）：
├── 高品质/限定服装图
├── 熟客故事插图
├── 活动专属资源
├── 背景音乐（streaming）
└── 后续更新内容
```

2. PreloadScene 加载流程

```typescript
class PreloadScene extends Phaser.Scene {
  preload(): void {
    // 显示加载进度
    this.createProgressBar();

    // 核心图集
    this.load.atlas('flowers', 'atlas/flowers.png', 'atlas/flowers.json');
    this.load.atlas('buildings', 'atlas/buildings.png', 'atlas/buildings.json');
    this.load.atlas('customers', 'atlas/customers.png', 'atlas/customers.json');
    this.load.atlas('ui', 'atlas/ui.png', 'atlas/ui.json');

    // 花店场景
    this.load.image('shop_bg_default', 'images/shop_bg_default.png');
    this.load.image('counter_default', 'images/counter_default.png');

    // 店主基础形象
    this.load.atlas('owner_base', 'dressup/owner_base.png', 'dressup/owner_base.json');

    // 音效
    this.load.audio('bgm_main', 'audio/bgm/main.mp3');
    this.load.audio('sfx_merge', 'audio/sfx/merge.mp3');
    this.load.audio('sfx_deliver', 'audio/sfx/deliver.mp3');
    this.load.audio('sfx_coin', 'audio/sfx/coin.mp3');
    this.load.audio('sfx_unlock', 'audio/sfx/unlock.mp3');
    this.load.audio('sfx_tap', 'audio/sfx/tap.mp3');

    // 数据配置
    this.load.json('flower_data', 'data/flowers.json');
    this.load.json('building_data', 'data/buildings.json');
    this.load.json('customer_data', 'data/customers.json');

    // 进度监听
    this.load.on('progress', (value: number) => {
      this.updateProgressBar(value);
    });
  }

  create(): void {
    // 初始化全局管理器
    GameDataManager.init(this.cache);
    CurrencyManager.init();
    AudioManager.init(this);

    // 加载存档
    const saveData = SaveManager.load();
    if (saveData) {
      GameDataManager.deserialize(saveData);
    }

    // 启动游戏
    this.scene.launch('UIScene');

    if (!saveData) {
      // 新玩家 → 直接进主场景 + 新手引导
      this.scene.start('MainScene', { isNewPlayer: true });
    } else {
      // 老玩家 → 开店仪式
      this.scene.start('OpenShopScene');
    }
  }
}
```

八、性能优化策略

1. 渲染优化

• 图集合并：所有小图使用 TexturePacker 合成图集，单图集不超过 2048×2048
• 对象池：频繁创建/销毁的对象使用对象池（花朵特效粒子、货币飘字、客人）
• Scene.sleep：非活跃页面 sleep 而非 destroy，减少重建开销
• 可见性裁剪：棋盘外的花朵不渲染（setVisible(false)）
• 批量渲染：同图集元素集中绘制，减少 DrawCall

2. 内存优化

• 按需加载：换装高清资源、装修高级装饰、活动资源走 CDN 懒加载
• 及时释放：切换 Scene 时释放非共享的大图纹理
• 图集复用：尽量多个元素共享同一图集
• 帧动画优化：客人走路等简单动画使用3~6帧，不追求高帧数

3. 逻辑优化

• 定时器合并：相同间隔的定时器合并处理（如多个建筑的 CD 使用同一个 1 秒 timer）
• 事件节流：拖拽过程中的碰撞检测每 3 帧检测一次，而非每帧
• 离线计算：离线收益在登录时一次性计算，不模拟离线过程
• 延迟初始化：图鉴/社交等二级页面首次进入时才初始化

九、开发排期建议（按 MVP 迭代）

阶段1：核心循环 MVP（3~4周）✅ 已完成
• 棋盘网格 + 物品拖拽合成（双品类：花束6级长链+花饮3级短链）
• 统一物品系统 BoardItem（替代旧版 FlowerItem）
• 基础建筑（花艺操作台，无产出线选择，无CD）
• 客人上门 + 组合订单（1~3需求槽位）+ 自动锁定 + 手动确认交付
• 锁定物品仍可拖拽合成（策划案核心策略点）
• 建筑解锁
• 基础 UI（顶部货币栏 + 棋盘）
• 本地存档
目标：跑通"合成→交付→赚金币→解锁建筑"核心循环

阶段2：完整合成经营（2~3周）✅ 已完成
• 三花系×6级 = 18种花束 + 三饮品线×3级 = 9种花饮 = 27种物品全部实现
• 全部7种建筑（4花束+3饮品）+ 产出线选择弹窗UI
• 16种客人 + 组合订单需求匹配（花+饮混合订单）
• 挂机双品类产出（遍历所有建筑自动产出）
• 离线收益（金币+物品）
• 饮品解锁进度控制（未解锁饮品建筑时只出纯花束需求客人）
• 新手引导（前3个阶段）[待实现]

阶段3：装修系统（2周）
• 装修 Scene + 花店全景
• 10个槽位交互
• 装饰解锁/切换/预览
• 花愿货币系统
• 主页面花店背景联动

阶段4：换装系统（2~3周）
• 换装 Scene + 立绘渲染器
• 分层换装逻辑
• 服装分类/解锁/试穿
• 花露货币系统
• 主页面店主形象联动
• 截图/分享

阶段5：社交 + 图鉴 + 任务（2周）
• 社交 Scene + 好友列表/排行榜
• 互访/助力/回赠
• 图鉴 Scene + 6大分类
• 熟客系统（好感度+故事）
• 任务系统

阶段6：打磨 + 微信适配（2周）
• 微信小游戏打包适配
• 激励视频广告接入
• 开店仪式/日结算
• 音效/BGM
• 全特效打磨
• 性能优化 + 真机测试

阶段7：商业化 + 活动系统（2周）
• 付费体系（格子扩容/月卡/礼包）
• 限时活动框架
• 活动花系合成
• 活动商店

总计预估：15~18周（约4个月）

十、关键风险与应对

| 风险 | 影响 | 应对方案 |
|------|------|---------|
| 微信小游戏 Phaser 适配问题 | 阻塞开发 | 阶段1第一周优先跑通 Phaser + 微信真机 |
| 换装立绘资源量大 | 包体超限/加载慢 | 严格走 CDN 懒加载，首次只加载基础服装 |
| 拖拽交互在低端机卡顿 | 体验差 | 减少拖拽时的实时特效，碰撞检测降频 |
| 排行榜数据安全 | 刷榜 | 关键数据服务端校验，客户端只做展示 |
| 存档数据损坏 | 玩家流失 | 双备份（本地+云端），版本迁移机制 |
| rex-ui 插件与微信环境兼容性 | 滚动列表异常 | 提前验证，备选方案用原生实现滚动 |
